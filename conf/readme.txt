Adjust the path in index_data.py to read from the csv directory.

This sample core should otherwise be already configured to run as a core in a Solr 8.x installation.

Install PySolr Python Solr client with pip install pysolr

Adjust the Solr core URL in index_data.py . Authentication may be required.

The core files needed are solrconfig.xml, schema.xml and index_data.py
